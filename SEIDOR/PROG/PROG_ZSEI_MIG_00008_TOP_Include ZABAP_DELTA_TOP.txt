*---------------------------------------------------------------------*
* Include          ZSEI_MIG_00008_TOP
*---------------------------------------------------------------------*

* Propias de la comparación de versiones
DATA:
  systemtype                 LIKE sy-sysid VALUE space,
  systemname                 LIKE sy-sysid VALUE space,
  log_dst1                   TYPE rfcdes-rfcdest,
  gv_ignore_case_differences TYPE c,
  one_column                 TYPE c,
  disp_mode                  TYPE c,
  with_linenumbers           TYPE c,
  comp_mode                  TYPE sy-index,
  delta_only                 TYPE c,
  shorten_same               TYPE c,
  ls_left_col                TYPE c,
  ls_right_col               TYPE c,
  split_edi                  TYPE c,
  with_korrnum               TYPE c,
  keep_pos                   TYPE c,
  remote_comp                TYPE c,
  left_flag                  TYPE c,
  double_remote_comp         TYPE c,
  sort_vers                  TYPE c,
  local_system               TYPE c,
  no_delta                   TYPE c,
  infoln1a                   LIKE vrsinfolna,
  infoln1b                   LIKE vrsinfolnb,
  infoln2a                   LIKE vrsinfolna,
  infoln2b                   LIKE vrsinfolnb,
  version_rgt                LIKE vrsd-versno,
  version_lft                LIKE vrsd-versno,
  objname_rgt                LIKE vrsd-objname,
  objname_lft                LIKE vrsd-objname,
  objtype_rgt                LIKE vrsd-objtype,
  objtype_lft                LIKE vrsd-objtype,
  korrnum_rgt                LIKE vrsd-korrnum,
  korrnum_lft                LIKE vrsd-korrnum,
  korrtxt_rgt                LIKE e07t-as4text,
  korrtxt_lft                LIKE e07t-as4text,
  korrinf_rgt                LIKE vrsinfolnb,
  korrinf_lft                LIKE vrsinfolnb,
  remsyst_lft                LIKE tadir-srcsystem,
  remsyst_rgt                LIKE tadir-srcsystem,
  logdest_lft                LIKE rfcdes-rfcdest,
  logdest_rgt                LIKE rfcdes-rfcdest,
  namlen                     TYPE i,
  abaptext_pri               LIKE abaptxt255 OCCURS 0 WITH HEADER LINE,
  abaptext_sec               LIKE abaptxt255 OCCURS 0 WITH HEADER LINE,
  abaptext_delta             LIKE vxabapt255 OCCURS 0 WITH HEADER LINE,
  abaptext_delta2            LIKE vxabapt255 OCCURS 0 WITH HEADER LINE,
  trdir_pri                  LIKE trdir      OCCURS 1 WITH HEADER LINE,
  trdir_old                  LIKE trdir_old  OCCURS 1 WITH HEADER LINE,
  trdir_sec                  LIKE trdir      OCCURS 1 WITH HEADER LINE,
  trdir_delta                LIKE xtrdir     OCCURS 1 WITH HEADER LINE,
  trdir_delta2               LIKE xtrdir     OCCURS 1 WITH HEADER LINE,
  textpool_pri               LIKE textpoolt  OCCURS 0 WITH HEADER LINE,
  textpool_sec               LIKE textpoolt  OCCURS 0 WITH HEADER LINE,
  textpool_delta             LIKE vxtextpool OCCURS 0 WITH HEADER LINE,
  textpool_delta2            LIKE vxtextpool OCCURS 0 WITH HEADER LINE,
  korrnum_tab                LIKE vrskorrnum OCCURS 0 WITH HEADER LINE,
  object_lft                 TYPE progdir-name,
  object_rgt                 TYPE progdir-name,
  gt_source_lft              TYPE rswsourcet,
  gt_source_rgt              TYPE rswsourcet,
  gv_transport_key           TYPE trkey,
  gt_smodilog_lft            TYPE TABLE OF smodilog,
  gt_smodilog_rgt            TYPE TABLE OF smodilog,
  gt_mod_tab_lft             TYPE smodi_mod_tab,
  gt_mod_tab_rgt             TYPE smodi_mod_tab,
  objname                    LIKE vrsd-objname,
  no_version                 TYPE c,
  objnam2                    LIKE vrsd-objname,
  objtyp1                    LIKE vrsd-objtype,
  objtyp2                    LIKE vrsd-objtype,
  versno1                    LIKE vrsd-versno,
  versno2                    LIKE vrsd-versno.

FIELD-SYMBOLS:
  <refname_lft>,
  <refname_rgt>.

* Propias del programas masivo de comparación de versiones
TYPES:
  BEGIN OF ty_excel,
    objname LIKE vrsd-objname,
    objtyp1 LIKE vrsd-objtype,
    versno1 LIKE vrsd-versno,
  END   OF ty_excel.

DATA:
  lt_filetable   TYPE filetable,
  ls_filetable   LIKE LINE OF lt_filetable,
  lv_rc          TYPE i,
  lt_excel       TYPE TABLE OF ty_excel,
  ls_excel       LIKE LINE OF lt_excel,
  objtype        LIKE vrsd-objtype,
  lv_orden       TYPE vrsd_old-korrnum,
  lv_description TYPE text60.

TYPES:
  BEGIN OF ty_alv,
    objname LIKE vrsd-objname,
    objtype LIKE vrsd-objtype,
    objtyp1 LIKE vrsd-objtype,
    versno1 LIKE vrsd-versno,
    delta   TYPE c,
    orden   TYPE vrsd_old-korrnum,
    desc    TYPE text60,
    icon    TYPE icon_d,
  END   OF ty_alv.

DATA:
  gt_fieldcat TYPE lvc_t_fcat,
  gs_fieldcat TYPE lvc_s_fcat,
  gs_layout   TYPE lvc_s_layo,
  gt_alv      TYPE STANDARD TABLE OF ty_alv,
  gs_alv      TYPE ty_alv,
  gx_variant  TYPE disvariant,
  g_variant   TYPE disvariant,
  g_repid     TYPE sy-repid,
  g_save      TYPE c VALUE 'A',
  g_exit      TYPE c.
